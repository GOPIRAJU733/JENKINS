pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Example build step
                echo 'Building...'
            }
        }
        stage('Deploy') {
            steps {
                // Example deployment step
                echo 'Deploying...'
            }
        }
    }
}
